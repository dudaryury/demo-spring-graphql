type Query {
    hello: String
    helloWithName(name: String): String
    customerById(id: ID): Customer
    customers: [Customer]
}

type Subscription {
    customersSubscription: Customer
}

type Customer {
    id: ID
    name: String
}