type Query {
    customerById(id:ID): Customer
    customers: [Customer]
}

type Mutation {
    addCustomer(name: String): Customer
}

type Customer {
    id: ID
    name: String
    account: Account
}

type Account {
    id: ID
}